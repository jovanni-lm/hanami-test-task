%h2 All students

- if students.any?
  %table.table.table-hover#students
    %thead
      %tr
        %th #
        %th First Name
        %th Last Name
        %th Email
        %th Links
    %tbody
    - students.each do |student|
      %tr
        %th{scope: 'row'}= student.id
        %td= student.first_name
        %td= student.last_name
        %td= student.email
        %td= link_to 'Edit', routes.edit_student_path(id: student.id)
- else
  %p There are no students yet.
